trigger:  none
pr:
  branches:
    exclude:
      - '*'
pool:
  vmImage: ubuntu-latest

parameters:
  - name: Env
    default: dev
    values:
      - prod
      - dev
      - uat

variables:
- name: inVar
  value: my inline variable
- template: ../variables/${{ parameters.Env }}.yml
# - group: group-${{ parameters.Env }}

steps:
- script: |
    echo environnment is ${{ parameters.Env }}
    echo my SubscriptionID is $(subID)
    echo my in file var is $(inVar)
    echo mysupersecret


    


